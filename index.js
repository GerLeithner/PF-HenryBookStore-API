function laCajaDePandora(numero) {
  // proximamente escribiremos codigo aqui
  return n % 2 === 0 ? numero.toString(2) : numero.toString(16);
}

function andrea() {
  return {
    nombre: "Andrea",
    edad: "24",
    nacionalidad: "Argentina",
  };
}
